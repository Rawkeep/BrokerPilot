---
phase: 02-crm-pipeline
plan: 04
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - client/src/components/dashboard/KPIGrid.jsx
  - client/src/components/dashboard/KPICard.jsx
  - client/src/components/dashboard/PipelineChart.jsx
  - client/src/components/dashboard/ConversionFunnel.jsx
  - client/src/components/dashboard/RecentActivityFeed.jsx
  - client/src/components/pages/DashboardPage.jsx
  - client/src/styles/dashboard.css
  - client/src/main.jsx
  - client/src/i18n/de.js
autonomous: true
requirements:
  - CRM-06

must_haves:
  truths:
    - "User can view total pipeline value as a KPI card in EUR"
    - "User can view conversion rate (Abschlussquote) as a percentage"
    - "User can view active deals count"
    - "User can view a bar chart showing leads and value per pipeline stage"
    - "User can view a conversion funnel visualization"
    - "User can view a recent activity feed showing last 20 events across all leads"
    - "Dashboard data updates when leads change (derived from leadStore)"
  artifacts:
    - path: "client/src/components/dashboard/KPIGrid.jsx"
      provides: "Dashboard layout with KPI cards and chart sections"
      min_lines: 40
    - path: "client/src/components/dashboard/KPICard.jsx"
      provides: "Single KPI metric card with label, value, optional trend indicator"
      min_lines: 15
    - path: "client/src/components/dashboard/PipelineChart.jsx"
      provides: "Recharts BarChart showing leads per stage with value"
      min_lines: 30
    - path: "client/src/components/dashboard/ConversionFunnel.jsx"
      provides: "Funnel/bar chart showing lead drop-off per stage"
      min_lines: 30
    - path: "client/src/components/dashboard/RecentActivityFeed.jsx"
      provides: "List of last 20 activities across all leads with links"
      min_lines: 30
    - path: "client/src/components/pages/DashboardPage.jsx"
      provides: "Replaces placeholder with full KPI dashboard"
      min_lines: 40
  key_links:
    - from: "client/src/components/dashboard/KPIGrid.jsx"
      to: "client/src/stores/leadStore.js"
      via: "useLeadStore.getKPIs for computed metrics"
      pattern: "getKPIs"
    - from: "client/src/components/dashboard/PipelineChart.jsx"
      to: "recharts"
      via: "BarChart, Bar, XAxis, YAxis, Tooltip, ResponsiveContainer"
      pattern: "import.*from.*recharts"
    - from: "client/src/components/dashboard/RecentActivityFeed.jsx"
      to: "date-fns"
      via: "formatDistanceToNow with de locale"
      pattern: "formatDistanceToNow"
---

<objective>
Build the KPI dashboard with real computed metrics, pipeline charts, and activity feed. Replace the DashboardPage placeholder with a data-driven view that gives brokers an instant overview of their business.

Purpose: The dashboard is the first thing brokers see — it must show actionable KPIs derived from their actual pipeline data.
Output: Working dashboard with KPI cards, pipeline bar chart, conversion funnel, and recent activity feed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-crm-pipeline/02-RESEARCH.md
@.planning/phases/02-crm-pipeline/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 02-01: leadStore and KPI computation -->

From client/src/stores/leadStore.js:
```javascript
export const useLeadStore = create((set, get) => ({
  leads: [],
  loading: true,
  async init() {},
  getKPIs(brokerType, stages) {
    // Returns:
    // {
    //   pipelineValue: number (EUR),
    //   conversionRate: number (0-1),
    //   activeDeals: number,
    //   perStage: [{ stage: string, label: string, count: number, value: number }],
    //   recentActivity: [{ id, type, timestamp, description, metadata, leadId, leadName }]
    // }
  },
}));
```

From shared/brokerTypes.js:
```javascript
BROKER_TYPES[brokerType].pipelineStages // [{ id, label, order }]
```

From existing UI:
```javascript
// GlassCard, GlassButton, GlassBadge — available
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Recharts, create KPICard, PipelineChart, ConversionFunnel, RecentActivityFeed</name>
  <files>client/src/components/dashboard/KPICard.jsx, client/src/components/dashboard/PipelineChart.jsx, client/src/components/dashboard/ConversionFunnel.jsx, client/src/components/dashboard/RecentActivityFeed.jsx, client/src/styles/dashboard.css, client/src/main.jsx, client/src/i18n/de.js</files>
  <action>
1. Install Recharts:
```bash
cd /Users/z_rkb/Downloads/BrokerPilot && npm install recharts
```

2. Create client/src/components/dashboard/KPICard.jsx:
   - Props: title (string), value (string|number), subtitle (optional string), icon (optional emoji string), trend (optional: 'up'|'down'|'neutral')
   - Renders inside a GlassCard: icon top-left, title (muted label), value (large bold text), subtitle below, trend arrow indicator (green up, red down, gray neutral)
   - For currency values, format with Intl.NumberFormat('de-DE', { style: 'currency', currency: 'EUR' })
   - For percentage values, format with Intl.NumberFormat('de-DE', { style: 'percent', maximumFractionDigits: 1 })

3. Create client/src/components/dashboard/PipelineChart.jsx:
   - Props: data (perStage array from KPIs: [{stage, label, count, value}])
   - Renders a Recharts ResponsiveContainer (width 100%, height 300) containing a BarChart
   - Two bars: count (primary color) and value (accent color, secondary Y axis)
   - XAxis: stage labels. YAxis left: count. YAxis right: value in EUR.
   - Tooltip: shows stage label, lead count, and value formatted as EUR
   - Use CSS variables for colors: var(--color-accent), var(--color-text-secondary)
   - Handle empty data: show "Keine Daten" placeholder text

4. Create client/src/components/dashboard/ConversionFunnel.jsx:
   - Props: data (perStage array), total (total lead count)
   - Renders a horizontal funnel visualization using Recharts BarChart with bars getting progressively smaller (natural pipeline drop-off)
   - Each bar shows: stage label, count, percentage of total
   - Alternative: Use a simple stacked view if funnel is complex — stage label on left, bar width proportional to count, percentage on right
   - Colors gradient from first stage color to closed stage color
   - Handle empty data gracefully

5. Create client/src/components/dashboard/RecentActivityFeed.jsx:
   - Props: activities (recentActivity array from KPIs: [{id, type, timestamp, description, metadata, leadId, leadName}])
   - Renders a list of up to 20 activity entries
   - Each entry: icon (per activity type, same as ActivityTimeline from Plan 02-03), lead name as clickable Link to /pipeline/:leadId, description, relative timestamp (formatDistanceToNow with de locale)
   - Empty state: "Keine Aktivitäten vorhanden"
   - Scrollable container with max-height

6. Create client/src/styles/dashboard.css:
   - `.dashboard-grid`: CSS grid — 3 columns for KPI cards on desktop, 1 on mobile, gap 1rem
   - `.dashboard-charts`: 2 columns on desktop (pipeline chart + funnel), 1 on mobile
   - `.kpi-card`: padding, flex column, min-height 120px
   - `.kpi-card__icon`: font-size 1.5rem
   - `.kpi-card__title`: muted text, font-size 0.85rem, uppercase
   - `.kpi-card__value`: font-size 2rem, font-weight 700
   - `.kpi-card__subtitle`: muted text, font-size 0.8rem
   - `.kpi-card__trend--up`: color green, `.kpi-card__trend--down`: color red
   - `.activity-feed`: max-height 400px, overflow-y auto
   - `.activity-feed__item`: flex row, padding 0.5rem 0, border-bottom subtle
   - `.activity-feed__lead-name`: font-weight 600, clickable
   - `.activity-feed__time`: muted, font-size 0.8rem, margin-left auto
   - Respect [data-theme] for dark/light

7. Import dashboard.css in client/src/main.jsx.

8. Extend de.js (add to crm.kpis if not already present):
   - `crm.kpis.noData`: 'Keine Daten vorhanden'
   - `crm.kpis.noActivity`: 'Keine Aktivitäten vorhanden'
   - `crm.kpis.leadCount`: 'Leads'
   - `crm.kpis.stageValue`: 'Wert'
   - `crm.kpis.conversionFunnel`: 'Conversion-Trichter'
   - `crm.kpis.pipelineOverview`: 'Pipeline-Übersicht'
  </action>
  <verify>
    <automated>cd /Users/z_rkb/Downloads/BrokerPilot && node -e "import('recharts').then(() => console.log('recharts OK')).catch(e => { console.error(e); process.exit(1) })" && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>Recharts installed. KPICard, PipelineChart, ConversionFunnel, RecentActivityFeed components created. Dashboard styles in dashboard.css imported. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Build KPIGrid and wire DashboardPage with real data</name>
  <files>client/src/components/dashboard/KPIGrid.jsx, client/src/components/pages/DashboardPage.jsx</files>
  <action>
1. Create client/src/components/dashboard/KPIGrid.jsx:
   - Reads from useLeadStore (leads, loading, init, getKPIs)
   - Reads from useSettingsStore (brokerType)
   - Gets pipeline stages from BROKER_TYPES[brokerType]?.pipelineStages
   - Calls leadStore.init() in useEffect on mount (if not already loaded)
   - Computes KPIs via getKPIs(brokerType, stages)
   - If no broker type selected: show message to select one (like PipelinePage)
   - If loading: show skeleton cards (3 GlassCards with pulsing animation)
   - Layout (top to bottom):
     a. KPI cards row (3 cards, dashboard-grid):
        - Pipeline-Wert: KPICard with pipelineValue formatted as EUR
        - Abschlussquote: KPICard with conversionRate as percentage
        - Aktive Deals: KPICard with activeDeals count
     b. Charts row (2 columns, dashboard-charts):
        - Left: PipelineChart with perStage data, title "Pipeline-Übersicht"
        - Right: ConversionFunnel with perStage data and total lead count, title "Conversion-Trichter"
     c. Activity feed (full width):
        - RecentActivityFeed with recentActivity data, title "Letzte Aktivitäten"
   - Wrap each section in GlassCard with appropriate padding

2. Replace client/src/components/pages/DashboardPage.jsx:
   - Remove placeholder content
   - Render: page title "Dashboard" + subtitle from de.js, then KPIGrid component
   - The page just provides the header; KPIGrid handles all data and layout

3. Ensure the dashboard is reactive: when leads change (add, move, delete) in another tab or after navigation, the KPIs recompute automatically (Zustand selectors ensure this — getKPIs reads from get().leads which triggers re-render on leads change).
  </action>
  <verify>
    <automated>cd /Users/z_rkb/Downloads/BrokerPilot && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>DashboardPage shows real KPI cards (pipeline value, conversion rate, active deals) computed from leads. Pipeline bar chart shows leads per stage. Conversion funnel shows drop-off. Recent activity feed shows last 20 events with links to leads. All data-driven from leadStore. Empty state handled. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lead data -> chart rendering | KPI values derived from leads rendered in charts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Denial of Service | Recharts with large dataset | accept | <10K leads renders in <100ms. No mitigation needed for v1 local-first scale. |
| T-02-11 | Information Disclosure | KPI values visible | accept | Client-side app, user's own data displayed to them. No disclosure risk. |
</threat_model>

<verification>
1. Start dev server, select a broker type in Einstellungen
2. Create several leads in Pipeline with varying stages, values, priorities
3. Navigate to Dashboard:
   - KPI cards show correct pipeline value (sum of non-closed dealValues), conversion rate, active deals count
   - Pipeline chart shows bar chart with stages and counts
   - Conversion funnel shows lead distribution across stages
   - Recent activity feed shows creation events with lead names (clickable)
4. Move a lead to "Abgeschlossen" in Pipeline, return to Dashboard — conversion rate increases
5. Empty state: with no leads, dashboard shows zeros and "Keine Daten" messages
</verification>

<success_criteria>
- DashboardPage replaces placeholder with real data-driven KPI dashboard
- Three KPI cards: Pipeline-Wert (EUR), Abschlussquote (%), Aktive Deals (count)
- Pipeline bar chart shows leads and value per stage via Recharts
- Conversion funnel visualizes lead drop-off across stages
- Recent activity feed shows last 20 events with links to lead detail pages
- All values computed from leadStore (no hardcoded data)
- Dashboard updates reactively when leads change
- Empty states handled gracefully
- All UI text in German
- Responsive layout (3-col KPIs -> 1-col on mobile)
- All tests pass (existing + new)
</success_criteria>

<output>
After completion, create `.planning/phases/02-crm-pipeline/02-04-SUMMARY.md`
</output>
